<%= form_with(model: orders_description) do |form| %>
  <% if orders_description.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(orders_description.errors.count, "error") %> prohibited this orders_description from being saved:</h2>

      <ul>
        <% orders_description.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :item_id, style: "display: block" %>
    <%= form.number_field :item_id %>
  </div>

  <div>
    <%= form.label :order_id, style: "display: block" %>
    <%= form.number_field :order_id %>
  </div>

  <div>
    <%= form.label :quantity, style: "display: block" %>
    <%= form.number_field :quantity %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
